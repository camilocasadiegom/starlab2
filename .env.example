# Copia esto a .env y rellena localmente (NO subir .env real)
# === Base URL por entorno ===
# Local PC/Termux sin Render:
BASE_URL=http://127.0.0.1:8000
# Si usas túnel (Cloudflare/ngrok), reemplazar con la URL pública HTTPS:
# BASE_URL=https://<tu-subdominio-o-url-del-tunel>

# Otros (no obligatorios de momento)
RENDER_API=
SERVICE_ID=
ADMIN_KEY=
DB_URL=
